Generic Text Document — E2E Test Steps
========================================

This file demonstrates the plaintext/generic parser.
It uses @begin/@end markers to identify test blocks.

Setup Steps
-----------

@begin(go-e2e-step step-name="Create test namespace")
kubectl create namespace e2e-test
@end

@begin(go-e2e-step step-name="Deploy test fixtures" timeout=60s)
kubectl apply -f ./testdata/fixtures/ -n e2e-test
@end

Verification Steps
------------------

@begin(go-e2e-step step-name="Verify pods are running")
kubectl get pods -n e2e-test -o wide
@end

@begin(go-e2e-step step-name="Run smoke test" timeout=30s)
curl -f http://localhost:8080/api/v1/status
@end

Teardown Steps
--------------

@begin(go-e2e-step step-name="Cleanup test namespace")
kubectl delete namespace e2e-test
@end
